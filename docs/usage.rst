========
Usage
========

To use djangocms-twitter2 in a project::

1. Add `djangocms_twitter` to `INSTALLED_APPS`::

    INSTALLED_APPS = (
        ...
        'djangocms_twitter',
        ...
    )
   